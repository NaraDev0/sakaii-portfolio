---
import { getAssetURL, type About } from "../lib/directus";

const { about, t } = Astro.props as { about: About; t: any };
const photoUrl = getAssetURL(about.photo);
const portraitLabel = t.about.portrait.replace("{name}", about.name);
---

<section class="section" id="about" data-reveal>
  <div class="container">
    <p class="section-label">{t.sections.about}</p>
    <div class="about-grid">
      <div class="portrait-frame" aria-label={portraitLabel}>
        {
          photoUrl ? (
            <img src={photoUrl} alt={portraitLabel} loading="lazy" />
          ) : (
            <div class="portrait-placeholder" role="img" aria-label={portraitLabel}>
              <span>MB</span>
            </div>
          )
        }
      </div>

      <div class="about-copy">
        <h2>{about.name}</h2>
        <h3>{about.title}</h3>
        <p>{about.bio}</p>

        <div class="stats-grid">
          {t.about.stats.map((stat: { value: string; label: string }) => (
            <div class="card stat-card">
              <strong>{stat.value}</strong>
              <span>{stat.label}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
